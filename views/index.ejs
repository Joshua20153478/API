<html>
    <head>
        <script src="https://code.jquery.com/jquery-3.5.1.min.js"
    integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>
    <title>Page Title</title>
    <link href="/assets/style.css" type="text/css" rel="stylesheet">
    </head>
<body>
    <form method="POST" action="/index">
        <a href="agregar1">Agregar</a>
        </form>
        <form method="POST" action="/index">
            <a href="buscar1">Buscar</a>
            </form>
    <table>
        <tr>
            <th>ID</th>
            <th>Nombre</th>
            <th>Tamaño</th>
            <th>Ingrediente</th>
            <th>Queso</th>
            <th>Pan</th>
            <th>Eliminar</th>
            <th>Editar</th>
        </tr>
        <% for(var i=0; i < data.length; i++) {%>
        <tr>
            <td><%= data[i]._id %></td>
            <td><%= data[i].name %></td>
            <td><%= data[i].size %></td>
            <td><%= data[i].ingrediente %></td>
            <td><%= data[i].queso %></td>
            <td><%= data[i].pan %></td>
            <td>
            <form method="DELETE" action="/eliminar">
                <button type = "submit" name = "name" value = "<%= data[i].name %>" >Eliminar</button>
            </form>
            </td>
            <td>
            <form method="PUT" action="/editar1/:id">
                <button type = "submit" name = "_id" value = "<%= data[i]._id %>" >Editar</button>
            </form>
            </td>
        </tr>
        <% } %>
        </tr>
    </table>



</body>

</html>